---
- name: "install and run the service"
  hostas: all
  become: yes
  tasks:
  - name: "installing the nginx"
    yum:
      name: nginx
      state: present
  - name: "staring the servcie"
    service:
      name: nginx
      state: started